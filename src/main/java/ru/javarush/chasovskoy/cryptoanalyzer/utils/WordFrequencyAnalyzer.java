package ru.javarush.chasovskoy.cryptoanalyzer.utils;

import java.util.*;
import java.util.stream.Collectors;

public class WordFrequencyAnalyzer {
    // takes tokens as input and returns a hashmap with the most frequent
    // words if they have length from 3 to 6 characters (top 10 in each category)
}
